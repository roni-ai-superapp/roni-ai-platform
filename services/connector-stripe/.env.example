# Stripe API Key (required)
STRIPE_SECRET_KEY=sk_test_...

# Connector Auth Token (optional - if not set, auth is bypassed for dev)
CONNECTOR_STRIPE_AUTH_TOKEN=

# Server Port (optional - default: 3002)
PORT=3002
